#include <bits/stdc++.h>
using namespace std;

int main() {
    int N;

    // Check if N is valid integer
    if(!(cin >> N)){
        cout << "Invalid Input";
        return 0;
    }
    
    return 0;
}